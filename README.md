# score-builder-users-service
User service for the Score Builder project.

Before running the app, make sure to edit `credentials_blank.py` with your own database credentials and rename it to `credentials.py`.
